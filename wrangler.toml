name = "babyplaypenhouse"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "frontend/dist"

[vars]
NODE_ENV = "production"

# Database: Turso (via TURSO_URL + TURSO_AUTH_TOKEN env secrets)
